<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icss.newretail.user.dao.StoreTypeMapper">

    <select id="queryStoreTypes" parameterType="string" resultType="com.icss.newretail.model.StoreTypeDTO">
        SELECT store_type ,store_type_name ,tenant_id ,create_time ,create_user ,update_time ,update_user
        FROM t_user_store_type
        WHERE tenant_id=#{tenantId}
        <if test="request.storeTypeName != null and request.storeTypeName != ''">
            <![CDATA[ AND LOCATE(#{request.storeTypeName},store_type_name) > 0 ]]>
        </if>
    </select>
</mapper>
